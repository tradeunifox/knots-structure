## Multi-Thread Model

A single KNOTS file may contain multiple threads.

Each thread acts as an independent parent root.
Together, multiple threads form a forest structure.

Knots are strictly bound to their own thread hierarchy.
